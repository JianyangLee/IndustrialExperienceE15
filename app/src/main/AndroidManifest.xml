<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.industrialexperiencee15">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DataVisual" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDVBtM1lQMcxHakdl_lmVq4w_7vjdpS6jA"/>

        <activity
            android:name=".Facilities"
            android:label="@string/title_activity_facilities"></activity>
        <activity
            android:name=".activity_exercise"
            android:windowSoftInputMode="stateVisible|adjustPan" />

        <activity android:name=".TrackFood" />
        <activity android:name=".OnBoard_CalculatedGoal_S5" />
        <activity android:name=".OnBoard_UserInfo_S4" />
        <activity android:name=".OnBoard_ActivityLevel_S3" />
        <activity android:name=".OnBoard_Goal_S2" />
        <activity android:name=".OnBoard_Welcome_S1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Dashboard"
            android:label="@string/Dashboard">
             <!--<intent-filter>-->
             <!--<action android:name="android.intent.action.MAIN" />-->
             <!--<category android:name="android.intent.category.LAUNCHER" />-->
             <!--</intent-filter>-->
        </activity>
        <activity
            android:name=".Tracker"
            android:label="@string/Tracker"
            android:windowSoftInputMode="stateVisible|adjustPan" />
        <activity
            android:name=".Signup"
            android:label="@string/Tracker" />
    </application>

</manifest>